<!DOCTYPE html>
<html>
  <head>
    <title>BarfWolves</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
<!-- Not doing a stylesheet, so I removed that  -->
  </head>

  <body>
    <%= yield %>
  </body>
</html>
